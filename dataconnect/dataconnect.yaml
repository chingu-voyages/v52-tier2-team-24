specVersion: "v1beta"
serviceId: "chingu-solar_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "chingu-solar_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
